# calendar

Canonical scheduling contracts and runtime primitives for Subactor.

Implementation is developed through governed tickets. The repository HOME is
`subactor`; it ADOPTs `wellmanifest/new-project` and does not redefine that
standard.
